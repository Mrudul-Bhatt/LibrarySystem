package com.example.LibrarySystem.ATMSystem.System3.Bank_BankAccount;

public class CurrentAccount extends BankAccount {

    public CurrentAccount(int accountNumber, double totalBalance, double availableBalance, int pin) {
        super(accountNumber, totalBalance, availableBalance, pin);
    }

}
